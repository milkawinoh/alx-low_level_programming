#include <stdio.h>

/**
 * main - Prints the program name.
 * @argc: shows number of arguments supplied to the program.
 * @argv: An array of pointers to arguments.
 *
 * Return:  o.
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
